[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "apache-port.sh"
healthcheckPath = "/healthcheck.php"
healthcheckTimeout = 20

[env]
HOTEL_DEMO_SESSION_ONLY = "true"